			<div class="tabbed-table-wrapper">
				<div <?php if (strlen(trim($container_id)) > 0): echo 'id="'.$container_id.'" '; endif; ?>>
				    <ul class="tabs">
				    	<?php // this is to hold the tabs for switching between tables, content set by JS ?>
				    </ul>
				</div>
			</div>